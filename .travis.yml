language: java
matrix:
  include:
    - os: linux
      jdk: oraclejdk8
    - os: osx
      -before_install:
        - brew update || brew install ant
      env: ANDROID_HOME=/usr/local/opt/android-sdk
      osx_image: xcode8
