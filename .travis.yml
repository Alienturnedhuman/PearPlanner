language: java
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

jdk:
  - oraclejdk8
addons:
  apt:
    packages:
      - oracle-java8-installer


deploy:
  provider: releases
  api_key: "$GIT_API"
  file: "build-travis.zip"
  skip_cleanup: true
  on:
    tags: true